# Environment Variables for Hero Meet

# LiveKit Configuration
LIVEKIT_URL=wss://your-livekit-url.livekit.cloud
LIVEKIT_API_KEY=your-livekit-api-key
LIVEKIT_API_SECRET=your-livekit-api-secret

# STT Configuration
NEXT_PUBLIC_STT_PROVIDER=webspeech
# Options: webspeech, deepgram

# Deepgram STT Configuration (client-side)
NEXT_PUBLIC_DEEPGRAM_API_KEY=your-deepgram-api-key

# Google Gemini LLM Configuration
GEMINI_API_KEY=your-gemini-api-key

# LLM Provider Selection
# Options: gemini, groq (default: gemini)
LLM_PROVIDER=gemini

# Groq LLM Configuration
GROQ_API_KEY=your-groq-api-key
# Optional: override default Groq model
GROQ_MODEL=meta-llama/llama-4-scout-17b-16e-instruct

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase Configuration (for transcript storage & vector search)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
# Note: Server-side keys (optional, for server-only operations)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key

# TTS Provider (optional)
# Options: gtts (default, free), elevenlabs (premium)
TTS_PROVIDER=gtts
ELEVENLABS_API_KEY=your-elevenlabs-api-key
